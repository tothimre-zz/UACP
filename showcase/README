.settings .project .buildpath
    Entryes contains eclipse project informations and settings

example_01
    contains a reference implementation of the auth box It uses $_SESSION 
    enviroment variable to stre the login information without design, but a 
    later easily designed interface.